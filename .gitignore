node_modules/
src/js/*
.vscode/settings.json
.idea/*
.DS_Store

# APM dependencies
apm_modules/

# APM lockfile (no external dependencies; only contains generated_at and local file hashes)
apm.lock.yaml

# APM compile artifacts (generated from .apm/ — run `apm compile` to regenerate)
AGENTS.md
CLAUDE.md
.claude/rules/
.github/instructions/
