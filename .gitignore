node_modules/
src/js/*
.vscode/settings.json
.idea/*
.DS_Store

# APM の依存関係
apm_modules/

# APM のロックファイル (外部依存なし。generated_at とローカル生成物のハッシュのみを含むため追跡しない)
apm.lock.yaml

# APM コンパイル成果物 (.apm/ から `apm compile` で再生成されるため追跡しない)
# 例外: .github/instructions/ は Copilot Code Review がリポジトリから読むため、コミットして追跡する
AGENTS.md
CLAUDE.md
.claude/rules/
